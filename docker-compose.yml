version: '3'
services:
  dev:
    build: support/dev
    volumes:
      - .:/work
      - node-12-node-modules:/work/node_modules
      - yarn-cache-12:/tmp/yarn-cache
    working_dir: /work
    environment:
      yarn_cache_folder: /tmp/yarn-cache

volumes:
  node-12-node-modules:
    external: true
  yarn-cache-12:
    external: true
